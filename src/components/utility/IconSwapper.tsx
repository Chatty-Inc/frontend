import {Component} from 'react';
import {ThemeCtx} from '../core/UIThemeProvider';

/**
 * 
 */
export default class IconSwapper extends Component<{}, {}> {
    static contextType = ThemeCtx;

    render() {
        return <></>
    }
}